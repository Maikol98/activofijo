import 'package:flutter/material.dart';

class RevisionTecnicaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('revision')),
    );
  }
}